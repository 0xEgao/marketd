# Coinswap Market

Web interface for viewing Coinswap market offers.

## Running the Project

**Backend (Rust):**
```bash
cd daemon
cargo run
```

**Frontend (React):**
```bash
cd web
npm install
npm run dev
```

Open http://localhost:5173
